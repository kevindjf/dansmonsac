name: streak
description: "Streak tracking module for bag preparation habit formation"
version: 0.0.1
homepage:

environment:
  sdk: ^3.6.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  riverpod: ^2.6.1
  # the annotation package containing @riverpod
  riverpod_annotation: ^2.6.1
  flutter_riverpod: ^2.6.1
  dartz: ^0.10.1
  drift: ^2.28.2
  uuid: ^4.5.1

  common:
    path: ../common

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.9
  # the code generator
  riverpod_generator: ^2.6.2


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
